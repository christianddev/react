describe("demo.tets.js", () =>{
    test('should equals', () => {
        // 1. Inicialización
        const msg1 = 'abc';
    
        // 2. Estimulo
        const msg2 = 'abc';
    
        // 3. Observar el comportamiento
        expect(msg1).toBe(msg2);
    })
})
